{
  "2": {
    "approach": [
      "Elementary-school addition over two linked lists, digit by digit, carrying as you go.",
      "Use a dummy head so the first node needs no special case; `tail` tracks the last appended node.",
      "Loop while either list has nodes OR carry != 0; take each digit (0 if a list is exhausted), sum = x + y + carry.",
      "Store sum%10 in a new node, carry = sum/10; return dummy->next."
    ],
    "time": "O(max(m, n))",
    "space": "O(max(m, n)) for the result list"
  },
  "4": {
    "approach": [
      "Binary search the partition point, always on the SMALLER array, so it runs in O(log(min(m,n))).",
      "For a cut m in nums1 and n = (a+b)//2 - m in nums2, compute the 4 border values left1/right1/left2/right2 with -inf/+inf sentinels at the edges.",
      "A partition is valid when left1 <= right2 and left2 <= right1.",
      "Even total -> average of max(lefts) and min(rights); odd total -> min(rights).",
      "If left1 > right2 move the cut left, else move it right."
    ],
    "comments": [
      "`flag` distinguishes even vs odd total length."
    ],
    "time": "O(log(min(m, n)))",
    "space": "O(1)"
  },
  "7": {
    "approach": [
      "Pop digits off the end with %10 and rebuild the reversed number, x = x/10 each step.",
      "Before doing y = y*10 + t, check against INT_MAX/INT_MIN/10 bounds and bail out with 0 on overflow.",
      "Negatives work directly because C++ / and % keep the sign of the dividend."
    ],
    "time": "O(log10(x))",
    "space": "O(1)"
  },
  "18": {
    "approach": [
      "Generalized kSum by recursion: sort, then recursively fix one index at a time until k == 2.",
      "`quad` holds the currently fixed numbers; skip duplicates at each level to avoid repeated results.",
      "Base case k == 2 is a two-pointer scan from both ends toward the remaining target, skipping duplicate left values after a hit."
    ],
    "time": "O(n^3)",
    "space": "O(n) recursion depth + output"
  },
  "19": {
    "approach": [
      "Two-pointer gap method with a dummy node before head (handles removing the head itself).",
      "Advance the fast pointer r by n-1 steps first.",
      "Then move l (from dummy) and r together until r is the last node; l is now just before the target.",
      "Unlink with l->next = l->next->next."
    ],
    "time": "O(L)",
    "space": "O(1)"
  },
  "20": {
    "approach": [
      "Push every opening bracket onto a stack.",
      "On a closing bracket, fail if the stack is empty or its top isn't the matching opener; otherwise pop.",
      "Valid iff the stack is empty at the end."
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "31": {
    "approach": [
      "Scan from the right for the first i with nums[i] < nums[i+1] (the pivot).",
      "If a pivot exists, find the rightmost j > i with nums[j] > nums[i] and swap them.",
      "Reverse the suffix after i so it becomes ascending (smallest arrangement) -> the next permutation.",
      "No pivot means it's the last permutation; reversing the whole array wraps to the first."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "34": {
    "approach": [
      "Two binary searches over the sorted array.",
      "findFirst: on a match, record mid and keep searching LEFT (r = mid-1).",
      "findEnd: on a match, record mid and keep searching RIGHT (l = mid+1).",
      "Return [first, last]; both -1 when the target is absent."
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "36": {
    "approach": [
      "Track seen digits per row, per column, and per 3x3 box with hash sets (box keyed by (i//3, j//3)).",
      "Skip '.' cells; for a filled cell, it's invalid if the value already appears in its row/col/box set.",
      "Otherwise add it to all three sets."
    ],
    "time": "O(1) (fixed 9x9 board)",
    "space": "O(1)"
  },
  "48": {
    "approach": [
      "Rotate 90 degrees clockwise via a helper matrix.",
      "Build the transpose: m1[j][i] = matrix[i][j].",
      "Write back reversing each row: matrix[i][j] = m1[i][n-1-j]."
    ],
    "comments": [
      "Uses O(n^2) extra space; the in-place transpose + row-reverse variant is O(1)."
    ],
    "time": "O(n^2)",
    "space": "O(n^2)"
  },
  "54": {
    "approach": [
      "Keep four shrinking boundaries: left, right, top, bottom.",
      "Each layer: top row L->R (top++), right column T->B (right--), guard-check, bottom row R->L (bottom--), left column B->T (left++).",
      "The inner guard `if not (left<right and top<bottom): break` avoids re-reading a middle row/column on odd sizes."
    ],
    "time": "O(m*n)",
    "space": "O(1) extra"
  },
  "56": {
    "approach": [
      "Sort intervals by start.",
      "If the current interval starts within the last merged one (st <= res[-1][1]), extend its end to max(end, current end).",
      "Otherwise it's disjoint -> append as a new interval."
    ],
    "time": "O(n log n)",
    "space": "O(n) output"
  },
  "61": {
    "approach": [
      "Walk to the tail counting length, then close the list into a cycle (tail->next = head).",
      "Effective rotation is k % length; the new tail is (length - k%length) steps from the old tail.",
      "Step forward that many times, set new head = curr->next, then break the cycle (curr->next = NULL)."
    ],
    "time": "O(L)",
    "space": "O(1)"
  },
  "73": {
    "approach": [
      "First pass records which rows and columns contain a 0 into two sets.",
      "Second pass zeroes any cell whose row or column was marked.",
      "Two passes avoid a freshly written 0 cascading into more zeros."
    ],
    "comments": [
      "O(m+n) extra space; the O(1) version reuses the first row/column as markers."
    ],
    "time": "O(m*n)",
    "space": "O(m+n)"
  },
  "75": {
    "approach": [
      "Counting sort over the three values: count how many 0s, 1s, 2s.",
      "Overwrite the array in place: the 0s, then the 1s, then the 2s."
    ],
    "comments": [
      "Two-pass counting; the one-pass Dutch National Flag (three pointers) also works."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "81": {
    "approach": [
      "Rotated binary search that tolerates duplicates.",
      "When nums[l]==nums[mid] or nums[r]==nums[mid] you can't tell which half is sorted, so shrink that end by one (this is what makes the worst case O(n)).",
      "Otherwise find the sorted half (nums[l] < nums[mid] => left sorted) and check whether target lies inside it to pick a side."
    ],
    "time": "O(log n) average, O(n) worst",
    "space": "O(1)"
  },
  "83": {
    "approach": [
      "Single pass; because the list is sorted, duplicates are adjacent.",
      "When curr->val == curr->next->val, splice out and delete the next node; otherwise advance curr."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "118": {
    "approach": [
      "Build row by row starting from [[1]].",
      "Pad the previous row with a 0 on each side; each new entry is temp[j] + temp[j+1]."
    ],
    "time": "O(numRows^2)",
    "space": "O(numRows^2) output"
  },
  "128": {
    "approach": [
      "Put all numbers in a set for O(1) lookup.",
      "Only start counting a run at a number whose predecessor (num-1) is absent -> a sequence start.",
      "Extend while num+length is in the set; track the best length.",
      "Each number is touched at most twice, so it's O(n)."
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "141": {
    "approach": [
      "Floyd's tortoise & hare: slow moves 1 step, fast moves 2.",
      "If there's a cycle the fast pointer laps and meets slow; if fast hits the end there's no cycle."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "162": {
    "approach": [
      "Binary search on the slope; a peak is guaranteed by the nums[-1]=nums[n]=-inf framing.",
      "Handle both ends up front (strictly greater than the lone neighbor => peak).",
      "If the left neighbor is larger, a peak exists to the left (r=m); if the right neighbor is larger, one exists to the right (l=m); if both neighbors are smaller, mid is a peak."
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "169": {
    "approach": [
      "Boyer-Moore voting: keep a candidate and a count.",
      "When count hits 0, adopt the current number; +1 when it matches the candidate, -1 otherwise.",
      "The > n/2 element survives as the candidate."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "189": {
    "approach": [
      "k %= n so rotations wrap.",
      "Stash the last k elements, shift the first n-k elements right by k (iterate from the back to avoid overwrite), then drop the stashed elements into the front."
    ],
    "comments": [
      "O(k) extra; the triple-reverse trick does it in O(1) space."
    ],
    "time": "O(n)",
    "space": "O(k)"
  },
  "229": {
    "approach": [
      "Extended Boyer-Moore for > n/3 elements: at most two can qualify, so track two candidates and two counts.",
      "Run the usual vote update, then a verification pass (nums.count) confirms each candidate actually exceeds n/3."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "347": {
    "approach": [
      "Count frequencies in a hash map.",
      "Bucket sort by frequency: buckets[f] holds all values seen f times (max frequency is n).",
      "Walk buckets from high frequency down, collecting until k values are gathered.",
      "Avoids the O(n log n) of a heap/sort."
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "485": {
    "approach": [
      "Single-pass counter: +1 on a 1 and track the max; reset to 0 on any non-1."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "493": {
    "approach": [
      "Count reverse pairs (i<j with nums[i] > 2*nums[j]) as a by-product of merge sort.",
      "After recursing on both halves, countPairs sweeps the two sorted halves with a moving pointer to count valid pairs.",
      "The standard merge then sorts the range so parent calls see sorted halves."
    ],
    "time": "O(n log n)",
    "space": "O(n)"
  },
  "509": {
    "approach": [
      "Plain recursion straight off F(n) = F(n-1) + F(n-2) with base cases."
    ],
    "comments": [
      "Exponential without memoization; an iterative/DP version is O(n). n=0 and n=1 handled explicitly."
    ],
    "time": "O(2^n) (naive recursion)",
    "space": "O(n) recursion depth"
  },
  "540": {
    "approach": [
      "Binary search on index parity: in a fully paired sorted array, pairs start at even indices; the single element breaks that parity.",
      "Use the segment sizes around mid to decide which half still pairs evenly and move toward the half where the parity is broken."
    ],
    "comments": [
      "The code special-cases the small windows right around mid explicitly."
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "560": {
    "approach": [
      "Prefix sum + hash map of prefix-sum frequencies, seeded with {0:1}.",
      "For running prefix p, the number of subarrays ending here with sum k is how many times (p-k) has appeared.",
      "Add that to the answer, then record p."
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "645": {
    "approach": [
      "One pass with a set finds the duplicated number y and the running total.",
      "Missing z = ideal_sum(1..n) + y - actual_total (adding y back cancels the duplicate's double count).",
      "Return [duplicate, missing]."
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "875": {
    "approach": [
      "Binary search on the eating speed (the answer), range [1, max(pile)].",
      "For speed m, hours = sum(ceil(pile/m)); feasible if <= h.",
      "Feasible -> try slower (r=m-1); else faster (l=m+1). l converges to the minimum workable speed."
    ],
    "time": "O(n log(max pile))",
    "space": "O(1)"
  },
  "1011": {
    "approach": [
      "Binary search on ship capacity, range [max(weight), sum(weights)].",
      "willItCarry simulates loading day by day for a candidate capacity and reports whether it fits within `days`.",
      "Feasible -> shrink capacity; else grow. l is the minimum feasible capacity."
    ],
    "time": "O(n log(sum))",
    "space": "O(1)"
  },
  "1283": {
    "approach": [
      "Binary search on the divisor, range [1, max(nums)].",
      "For divisor m the cost is sum(ceil(num/m)); feasible when <= threshold.",
      "Feasible -> try a smaller divisor; else larger. l is the smallest divisor meeting the threshold."
    ],
    "time": "O(n log(max))",
    "space": "O(1)"
  },
  "1482": {
    "approach": [
      "If m*k > number of flowers it's impossible (-1).",
      "Binary search on days, range [1, max(bloomDay)]; getCount greedily counts complete bouquets of k adjacent bloomed flowers by a candidate day.",
      "Enough bouquets -> try fewer days; else more. l is the answer."
    ],
    "time": "O(n log(max bloom))",
    "space": "O(1)"
  },
  "1539": {
    "approach": [
      "Binary search: at index i, arr[i]-i-1 is how many positive integers are missing before arr[i].",
      "Find the boundary where the missing count first reaches k; the answer is l + k."
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "1552": {
    "approach": [
      "Sort positions, then binary search on the minimum gap (force), range [1, (max-min)//(m-1)].",
      "willItWork greedily places balls at least `mid` apart and checks whether all m fit.",
      "Feasible -> try a larger gap (l=mid+1); else smaller. Answer is l-1 (largest feasible minimum gap)."
    ],
    "time": "O(n log(range))",
    "space": "O(1)"
  },
  "1838": {
    "approach": [
      "Sort, then sliding window: making every element in the window equal to the largest (nums[r]) costs nums[r]*windowLen - windowSum.",
      "Grow r; while the cost exceeds k, shrink from the left.",
      "The largest valid window size is the answer."
    ],
    "time": "O(n log n)",
    "space": "O(1)"
  },
  "2149": {
    "approach": [
      "Result array with two write pointers: positives to even indices (0,2,4,...), negatives to odd indices (1,3,5,...).",
      "Single pass preserves relative order within each sign."
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "69": {
    "approach": [
      "Binary search for the largest integer whose square does not exceed x, over the range [1, x/2].",
      "Use a long for mid*mid to avoid 32-bit overflow; return mid immediately on an exact square.",
      "If mid*mid < x, record mid as the best answer so far and search right; otherwise search left.",
      "x < 2 is handled directly (sqrt of 0 or 1 is x itself)."
    ],
    "time": "O(log x)",
    "space": "O(1)"
  },
  "9": {
    "approach": [
      "Check if input number is negative",
      "Reverse the input number",
      "Compare reversed number with original number"
    ],
    "comments": [
      "Using long long to handle potential overflow when reversing the number"
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "136": {
    "approach": [
      "Initialize result variable to 0",
      "Iterate through each number in the input array",
      "XOR each number with the result",
      "Return the final result after iteration"
    ],
    "comments": [
      "This solution relies on the XOR property that a ^ a = 0 and a ^ 0 = a, allowing it to cancel out duplicate numbers"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "26": {
    "approach": [
      "Initialize a counter and a previous value",
      "Iterate through the input array",
      "If current number is greater than previous, update previous and increment counter",
      "Return the counter as the new length"
    ],
    "comments": [
      "The initial value of c is set to a very low number to ensure the first element is always included"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "283": {
    "approach": [
      "Initialize a nonzero flag",
      "Iterate through the array and move non-zero elements to the front",
      "Fill the rest of the array with zeros"
    ],
    "comments": [
      "This solution has a tradeoff between readability and performance, as it uses two separate loops to move non-zero elements and fill zeros, but achieves a linear time complexity"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "66": {
    "approach": [
      "Start from the end of the digits array",
      "Increment each digit and check for carry",
      "If no carry, return the updated array",
      "If all digits have carry, add a new most significant digit"
    ],
    "comments": [
      "The code handles the case where the input array represents the maximum possible number (e.g., [9,9,9]) by inserting a new digit at the beginning."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "242": {
    "approach": [
      "Check if input strings have the same length",
      "Create two hash maps to store character frequencies",
      "Iterate through both strings and update hash maps",
      "Compare the two hash maps for equality"
    ],
    "comments": [
      "Early length check avoids unnecessary hash map operations",
      "Using unordered_map allows for efficient character frequency counting"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "217": {
    "approach": [
      "Create an empty hash set",
      "Iterate through the input array",
      "Check for duplicates in the set",
      "Insert new elements into the set",
      "Return true if a duplicate is found"
    ],
    "comments": [
      "Using an unordered_set allows for efficient lookups with an average time complexity of O(1)"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "53": {
    "approach": [
      "Initialize current sum and max sum",
      "Iterate through array",
      "Reset current sum if negative",
      "Update current sum and max sum"
    ],
    "comments": [
      "This solution uses Kadane's algorithm, which is more efficient than a naive approach",
      "It handles negative numbers by resetting the current sum when it becomes negative"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "152": {
    "approach": [
      "Initialize max with the first element of the array",
      "Find the maximum single element in the array",
      "Use dynamic programming to track the maximum and minimum product ending at each position",
      "Update max with the maximum product found so far",
      "Handle zero elements by resetting the current max and min products"
    ],
    "comments": [
      "The initial max is updated to handle the case where the maximum product is a single element",
      "The use of currMin and currMax allows the algorithm to handle negative numbers, which can become maximum products when multiplied by another negative number"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "153": {
    "approach": [
      "Initialize two pointers, left and right, to the start and end of the array",
      "Perform binary search, calculating the middle index",
      "Compare the middle element to the rightmost element to determine which half to search",
      "Repeat until the left and right pointers converge"
    ],
    "comments": [
      "The key insight is that if the middle element is greater than the rightmost element, the minimum must be in the right half",
      "If the middle element is less than or equal to the rightmost element, the minimum could be in the left half or at the middle index"
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "33": {
    "approach": [
      "Initialize two pointers, left and right, to the start and end of the array",
      "Perform binary search, calculating the middle index and comparing the middle element to the target",
      "Determine which half of the array is sorted and adjust the search space accordingly",
      "Repeat the search process until the target is found or the search space is empty"
    ],
    "comments": [
      "The code handles the edge case where the input array is empty by immediately returning -1",
      "The use of 'l + (r-l)/2' instead of '(l+r)/2' avoids potential integer overflow issues"
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "167": {
    "approach": [
      "Initialize two pointers, one at the start and one at the end of the array",
      "Calculate the sum of the values at the two pointers",
      "Compare the sum to the target and adjust the pointers accordingly",
      "Repeat until the target sum is found"
    ],
    "comments": [
      "The use of while(1==1) is unconventional and could be replaced with a more standard while loop condition",
      "The function assumes that a solution will always be found, as per the problem statement"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "191": {
    "approach": [
      "initialize result variable",
      "loop until all bits are processed",
      "check least significant bit for 1",
      "increment result if bit is 1",
      "right shift to process next bit"
    ],
    "comments": [
      "This solution uses a simple iterative approach, but it could be optimized using bitwise operations like Brian Kernighan's algorithm."
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "704": {
    "approach": [
      "Initialize left and right pointers to the start and end of the array",
      "Calculate the middle index and compare the middle element to the target",
      "Update the left or right pointer based on the comparison result",
      "Repeat the process until the target is found or the search space is exhausted"
    ],
    "comments": [
      "The use of 'l + (r-l)/2' instead of '(l+r)/2' avoids potential integer overflow"
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "49": {
    "approach": [
      "Create a hashmap with character count as key",
      "Count character frequency for each string",
      "Store strings with same character count in hashmap",
      "Iterate hashmap to construct result"
    ],
    "comments": [
      "Assumes all strings are lowercase English letters",
      "Uses array of size 26 to count character frequency, assuming only lowercase letters"
    ],
    "time": "O(n*m)",
    "space": "O(n*m)"
  },
  "338": {
    "approach": [
      "Initialize a result vector with n+1 zeros",
      "Iterate over the range from 0 to n",
      "For each number, calculate its bit count using the previous bit count and the least significant bit",
      "Store the calculated bit count in the result vector",
      "Return the result vector"
    ],
    "comments": [
      "The expression i>>1 is used to get the bit count of the number without the least significant bit",
      "The expression i & 1 is used to get the least significant bit of the number"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "268": {
    "approach": [
      "Calculate the sum of all numbers from 0 to n (where n is the size of the input array)",
      "Calculate the sum of all numbers in the input array",
      "Find the missing number by subtracting the sum of the input array from the sum of all numbers"
    ],
    "comments": [
      "The commented-out code uses XOR operation to find the missing number, which is a more efficient approach",
      "The current implementation has a time complexity of O(n) due to the two separate loops for calculating sums"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "190": {
    "approach": [
      "initialize result variable",
      "iterate 32 times to process each bit",
      "left shift result and XOR with least significant bit of input",
      "right shift input to move to next bit"
    ],
    "comments": [
      "Assumes 32-bit integer, may not work for other sizes",
      "Uses bitwise operations for efficiency"
    ],
    "time": "O(1)",
    "space": "O(1)"
  },
  "74": {
    "approach": [
      "Perform binary search on the rows of the matrix to find the row where the target could be",
      "Check if the target is within the range of the current row",
      "If the target is within the range, perform binary search on that row to find the target",
      "If the target is not found after searching the row, return false"
    ],
    "comments": [
      "The code assumes the input matrix is sorted in ascending order from left to right and top to bottom",
      "The code uses two separate binary searches to first find the row and then the column where the target could be, which reduces the time complexity"
    ],
    "time": "O(log(m) + log(n))",
    "space": "O(1)"
  },
  "70": {
    "approach": [
      "Handle base cases for n = 0, 1, 2",
      "Initialize an array to store the number of ways to climb i stairs",
      "Fill the array using dynamic programming, where each step is the sum of the two preceding steps",
      "Return the number of ways to climb n stairs"
    ],
    "comments": [
      "The use of an array of size n+1 to store the number of ways to climb i stairs allows for efficient computation, but may cause issues for very large inputs due to memory constraints"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "322": {
    "approach": [
      "Initialize a DP table with size amount+1",
      "Fill the DP table using bottom-up dynamic programming",
      "For each amount from 1 to the target, try all coins",
      "Update the minimum number of coins needed for each amount",
      "Return the minimum number of coins needed for the target amount or -1 if impossible"
    ],
    "comments": [
      "The DP table is initialized with amount+1 to handle the case where it's impossible to make the target amount with the given coins"
    ],
    "time": "O(amount * n)",
    "space": "O(amount)"
  },
  "300": {
    "approach": [
      "Initialize LIS array with 1s",
      "Iterate through array from right to left",
      "For each element, compare with elements to its right",
      "Update LIS if a larger increasing subsequence is found"
    ],
    "comments": [
      "This solution has a time complexity of O(n^2) due to the nested loops",
      "It is not the most efficient solution for this problem, as a binary search approach can achieve O(n log n) time complexity"
    ],
    "time": "O(n^2)",
    "space": "O(n)"
  },
  "1752": {
    "approach": [
      "Iterate through the array to find elements that are smaller than their previous element",
      "Count the number of such elements and store it in the flag variable",
      "Check if the last element is greater than the first element and increment the flag if true",
      "Return true if the flag is 0 or 1, indicating the array is sorted and rotated"
    ],
    "comments": [
      "The code assumes that a rotated array can only be rotated once, which is a key insight to solve this problem efficiently"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "1143": {
    "approach": [
      "Initialize a 2D array to store lengths of common subsequences",
      "Fill the array in a bottom-up manner, comparing characters from both strings",
      "If characters match, increment the length of the common subsequence",
      "If characters do not match, choose the maximum length from the next cells",
      "The length of the longest common subsequence is stored in the top-left cell"
    ],
    "comments": [
      "The code uses a dynamic programming approach to avoid redundant computations",
      "The time complexity is optimized by filling the array in a single pass"
    ],
    "time": "O(m*n)",
    "space": "O(m*n)"
  },
  "27": {
    "approach": [
      "Initialize two pointers, one at the start and one at the end of the array",
      "Iterate through the array from left to right, checking for the target value",
      "When the target value is found, replace it with the value at the right pointer and decrement both pointers",
      "Continue iterating until the left pointer meets the right pointer",
      "Return the new length of the modified array"
    ],
    "comments": [
      "The code has a bug: the left pointer is decremented after being used as the loop condition, which can cause an infinite loop",
      "The correct implementation should not decrement the left pointer when replacing the target value"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "88": {
    "approach": [
      "Initialize pointers for nums1 and nums2 at their ends",
      "Compare elements and place the larger one at the end of nums1",
      "Repeat comparison until one of the arrays is exhausted",
      "Copy remaining elements from nums2 to nums1 if any"
    ],
    "comments": [
      "This solution modifies the input array nums1 in-place, which is more space-efficient than creating a new array",
      "It assumes that nums1 has enough space to hold all elements from both arrays"
    ],
    "time": "O(m + n)",
    "space": "O(1)"
  },
  "35": {
    "approach": [
      "Initialize two pointers, left and right, to the start and end of the array",
      "Perform binary search, calculating the mid index and comparing the target to the middle element",
      "If the target is found, return its index; otherwise, adjust the left or right pointer based on the comparison",
      "If the target is not found, return the left pointer, which represents the insertion point"
    ],
    "comments": [
      "The use of left + (right - left) / 2 instead of (left + right) / 2 avoids potential integer overflow"
    ],
    "time": "O(log n)",
    "space": "O(1)"
  },
  "13": {
    "approach": [
      "Create a hash map to store Roman numerals and their integer values",
      "Iterate through the input string",
      "Compare each numeral with the next one to determine whether to add or subtract its value"
    ],
    "comments": [
      "The code handles cases where a smaller numeral appears before a larger one (e.g., IV for 4) by subtracting the smaller numeral's value"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "1": {
    "approach": [
      "Create an unordered map to store numbers and their indices",
      "Iterate through the input array",
      "For each number, check if its complement exists in the map",
      "If it does, return the current index and the index of the complement",
      "If not, add the current number and its index to the map"
    ],
    "comments": [
      "Using an unordered map allows for constant time lookups, making the overall solution more efficient"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "121": {
    "approach": [
      "Initialize two pointers, left and right, to the start of the prices array",
      "Compare the difference between the prices at the right and left pointers",
      "Update the maximum profit if the difference is greater",
      "Move the pointers based on whether the difference is positive, negative, or zero",
      "Continue iterating until the right pointer reaches the end of the array"
    ],
    "comments": [
      "The code uses a two-pointer technique to find the maximum profit, but it does not explicitly use dynamic programming as the problem topic suggests",
      "The time complexity is optimized by only moving the pointers when necessary, avoiding unnecessary comparisons"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "238": {
    "approach": [
      "Initialize prefix and postfix arrays",
      "Calculate prefix products from left to right",
      "Calculate postfix products from right to left",
      "Combine prefix and postfix products for each element"
    ],
    "comments": [
      "The code has an issue with the line where prefix and postfix arrays are declared. In C++, when using variable length arrays (VLAs), the size must be a constant expression. Instead, consider using std::vector for dynamic array sizing."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "125": {
    "approach": [
      "Initialize two pointers, one at the start and one at the end of the string",
      "Skip non-alphanumeric characters from both ends",
      "Compare characters at the current positions, ignoring case",
      "Move pointers towards the center of the string",
      "Repeat until pointers meet or cross"
    ],
    "comments": [
      "The use of tolower() ensures the comparison is case-insensitive",
      "The isalnum() function is used to filter out non-alphanumeric characters"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "11": {
    "approach": [
      "Initialize two pointers at both ends of the array",
      "Calculate the area between the two pointers",
      "Update max area if current area is larger",
      "Move the pointer with the smaller height towards the center",
      "Repeat until the two pointers meet"
    ],
    "comments": [
      "The key insight is to move the pointer with the smaller height, because the area is limited by the smaller height",
      "This greedy approach ensures we don't miss the maximum area"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "15": {
    "approach": [
      "Sort the input array",
      "Iterate over the array with a pointer i",
      "Use two pointers, left and right, to find a triplet summing to zero",
      "Skip duplicate triplets",
      "Adjust pointers based on the sum"
    ],
    "comments": [
      "Early termination when nums[i] > 0 as the array is sorted and all subsequent triplets will have a sum greater than zero",
      "Duplicate skipping is crucial to avoid redundant triplets in the result"
    ],
    "time": "O(n^2)",
    "space": "O(n)"
  },
  "410": {
    "approach": [
      "Define a helper function willItWork to check if it's possible to split the array into k subarrays with a maximum sum of m",
      "Initialize the search range with the minimum possible sum (max(nums)) and the maximum possible sum (sum(nums))",
      "Perform binary search to find the minimum maximum sum",
      "Update the search range based on the result of willItWork",
      "Return the minimum maximum sum found"
    ],
    "comments": [
      "The willItWork function uses a greedy approach to try to split the array into k subarrays with a maximum sum of m",
      "The binary search approach is used to find the minimum maximum sum, which reduces the time complexity"
    ],
    "time": "O(n log sum)",
    "space": "O(1)"
  },
  "3": {
    "approach": [
      "Initialize a sliding window with left and right pointers",
      "Use a hash table to track the last seen index of each character",
      "Update the left pointer when a repeating character is found",
      "Update the maximum length of substring without repeating characters"
    ],
    "comments": [
      "The key insight is to move the left pointer to the right of the previous occurrence of the repeating character, not just to the next character",
      "This approach avoids unnecessary iterations and ensures the optimal solution"
    ],
    "time": "O(n)",
    "space": "O(min(n, m))"
  },
  "1004": {
    "approach": [
      "Initialize a sliding window with two pointers, l and r",
      "Expand the window to the right and count zeros",
      "Shrink the window from the left when zeros exceed k",
      "Update the maximum length of consecutive ones"
    ],
    "comments": [
      "The key insight is to maintain a window where the number of zeros does not exceed k",
      "This allows us to efficiently find the longest subarray with at most k zeros"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "904": {
    "approach": [
      "Initialize count dict, left pointer l=0, best=0",
      "Iterate over fruits with right index r and fruit f, increment count[f]",
      "If more than two fruit types in count, shrink window from left: decrement count of fruits[l], delete key if count becomes 0, and move l right",
      "Update best with current window size r-l+1",
      "Return best as the maximum window length"
    ],
    "comments": [
      "Handles empty input by returning 0",
      "Count dict never holds more than two keys, so space usage is O(1)"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "424": {
    "approach": [
      "Init left pointer l=0, result res=0, and empty count dict.",
      "Loop r over s with enumerate, increment count[ch].",
      "While window length minus max(count.values()) > k, decrement count[s[l]] and increment l.",
      "Update res = max(res, r-l+1) after window is valid.",
      "Return res."
    ],
    "comments": [
      "max(count.values()) is recomputed each iteration, costing O(Alphabet) time (constant 26 for lowercase letters).",
      "Zero-frequency entries stay in the dict but do not affect correctness."
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "1423": {
    "approach": [
      "Compute total sum of all cards",
      "If k >= n return total",
      "Initialize sum of first n-k cards as current window sum",
      "Slide window across array updating sum and track minimum window sum",
      "Return total minus the minimum window sum"
    ],
    "comments": [
      "Handles k equal to array length by early return; sliding window size zero works correctly"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "930": {
    "approach": [
      "Define helper findG(x) to count subarrays with sum ≤ x",
      "Initialize left pointer, current sum, and result",
      "Iterate right pointer, add nums[r] to current sum",
      "While current sum > x, move left pointer and subtract from sum",
      "Add (r-l+1) to result for each right position",
      "Return findG(goal)-findG(goal-1) as count of subarrays with sum exactly goal"
    ],
    "comments": [
      "Works only for non-negative (binary) arrays; handles goal=0 via early return",
      "Uses O(1) extra space"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "1248": {
    "approach": [
      "Define helper atMost(x) to count subarrays with ≤x odd numbers using sliding window",
      "Iterate right pointer over nums, increment oddCount on odd elements",
      "While oddCount exceeds x, move left pointer and decrement oddCount when passing an odd",
      "Add current window length (r-l+1) to result for each r",
      "Return atMost(k)-atMost(k-1) to get exactly k odds"
    ],
    "comments": [
      "Handles k=0 by returning 0 for atMost(-1)",
      "Uses O(1) extra space"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "992": {
    "approach": [
      "Define helper to count subarrays with at most x distinct",
      "Iterate right pointer, update frequency map",
      "Shrink left pointer while distinct count exceeds x",
      "Add (r-l+1) to result for each right"
    ],
    "comments": [
      "Handles x<0 by returning 0, so k=0 yields 0",
      "Uses O(k) extra space for the hashmap"
    ],
    "time": "O(n)",
    "space": "O(k)"
  },
  "240": {
    "approach": [
      "Determine the number of rows (m) and columns (n) of the matrix.",
      "Iterate over each row index i from 0 to m-1.",
      "If the first element of the current row exceeds the target, return False immediately (no later rows can contain the target).",
      "Otherwise, run a binary search on the current row between column indices l and r.",
      "If the target is found during the binary search, return True."
    ],
    "comments": [
      "The function may fall off the end without an explicit return, implicitly returning None instead of False when the target is not found.",
      "It assumes the matrix is non‑empty; calling it on an empty matrix would raise an IndexError."
    ],
    "time": "O(m log n)",
    "space": "O(1)"
  },
  "378": {
    "approach": [
      "Set n as matrix dimension and define isFeasible(mid) to count elements ≤mid by iterating rows bottom‑up and columns left‑to‑right, breaking when a value exceeds mid",
      "Initialize search bounds ansL and ansR to the smallest and largest matrix entries",
      "Binary‑search while ansL≤ansR: compute mid, call isFeasible(mid); if true move ansR left, else move ansL right",
      "After loop, return ansL as the kth smallest value"
    ],
    "comments": [
      "The double loop in isFeasible runs O(n²) in the worst case, so the overall complexity is O(n²·log (range)) rather than the optimal O(n·log range)",
      "Duplicates are handled because the check uses count≥k; the algorithm assumes matrix values fit in Python int range"
    ],
    "time": "O(n^2 log D)",
    "space": "O(1)"
  },
  "1901": {
    "approach": [
      "Initialize rows m and columns n, set binary search bounds lo=0, hi=n-1 on columns",
      "While lo≤hi, pick mid column and scan all rows to find the row index best with the maximum value in that column",
      "Compare mat[best][mid] with its left and right neighbours (using -1 as sentinel when out of bounds)",
      "If the current element is greater than both neighbours, return its coordinates as a peak",
      "Otherwise move the binary search to the side that has the larger neighbour (left → hi=mid-1, right → lo=mid+1)"
    ],
    "comments": [
      "Using -1 as a sentinel for missing neighbours assumes matrix values are non‑negative; a true negative value could break the comparison",
      "The algorithm runs in O(m log n) time and O(1) extra space"
    ],
    "time": "O(m log n)",
    "space": "O(1)"
  },
  "155": {
    "approach": [
      "Initialize empty list to hold (value,current_min) pairs",
      "On push compute new min as min(value, previous min) and append pair",
      "Pop removes last pair",
      "Top returns value part of last pair",
      "getMin returns min part of last pair"
    ],
    "comments": [
      "Methods assume stack is non‑empty; pop/top/getMin will raise IndexError on empty stack"
    ],
    "time": "O(1)",
    "space": "O(n)"
  },
  "496": {
    "approach": [
      "Map each nums1 value to its index",
      "Initialize result list with -1 and an empty stack",
      "Iterate over nums2, popping smaller stack elements and recording their next greater",
      "Assign the current nums2 element as the next greater for each popped value",
      "Push the current element onto the stack only if it appears in nums1"
    ],
    "comments": [
      "Assumes all numbers are distinct and that nums1 is a subset of nums2, as guaranteed by the problem"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "503": {
    "approach": [
      "Initialize result with -1 and an empty stack",
      "Iterate i from 0 to 2*n-1 treating the array as circular via nums[i%n]",
      "While the stack is non‑empty and nums[stack[-1]]<cur, pop index and set its result to cur",
      "Push i onto the stack only when i<n (first pass)",
      "Return the populated result list"
    ],
    "comments": [
      "Only indices from the first traversal are stored, so each element is considered exactly once as a candidate",
      "Strictly less comparison means equal values are not treated as greater, leaving -1 when no larger element exists"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "739": {
    "approach": [
      "Initialize empty stack and result array of zeros",
      "Iterate over each index i in temperatures",
      "While stack not empty and temperatures[i] > temperatures[stack[-1]], pop index and set res[index]=i-index",
      "Push i onto stack",
      "Return the result array"
    ],
    "comments": [
      "Works for empty or single‑element input; uses a monotonic decreasing stack to ensure each index is processed at most once"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "84": {
    "approach": [
      "Initialize maxSize and an empty stack storing (start index, height) pairs",
      "Iterate over heights with index i and height n, setting start=i",
      "While stack is non‑empty and n is less than the height at the top, pop (index,h), compute area h*(i-index), update maxSize, and set start=index",
      "Push (start,n) onto the stack",
      "After the loop, for each remaining (index,h) in the stack compute area h*(len(heights)-index) and update maxSize",
      "Return maxSize"
    ],
    "comments": [
      "Handles empty input returning 0; equal heights are kept to avoid redundant area calculations"
    ],
    "time": "O(n)",
    "space": "O(n)"
  },
  "42": {
    "approach": [
      "Initialize left and right pointers at the array ends and set maxL and maxR to the heights at those positions",
      "Loop while left < right, comparing maxL and maxR",
      "If maxL <= maxR, increment left, update maxL to the larger of current maxL and new height, and add maxL - height[left] to result",
      "Else, decrement right, update maxR similarly, and add maxR - height[right] to result"
    ],
    "comments": [
      "Returns 0 for an empty list",
      "Uses only constant extra variables, so space is O(1)"
    ],
    "time": "O(n)",
    "space": "O(1)"
  },
  "200": {
    "approach": [
      "Iterate over each cell in the grid",
      "When an unvisited land cell ('1') is encountered, launch a BFS from it",
      "Use a deque to pop cells and push any adjacent land cells (up, down, left, right) that are inside bounds and not yet visited",
      "Mark every visited cell in a set to avoid re‑processing",
      "After the BFS empties, increment the island counter"
    ],
    "comments": [
      "Assumes grid is non‑empty; accessing grid[0] will raise on an empty list",
      "The inner loop reassigns the variables r and c, but this shadowing does not affect the outer loops"
    ],
    "time": "O(m*n)",
    "space": "O(m*n)"
  },
  "733": {
    "approach": [
      "If starting pixel already has target color, return image",
      "Store original color and initialize BFS queue with starting coordinates",
      "Mark starting pixel with new color",
      "While queue not empty, pop a pixel and examine its four neighbors",
      "If neighbor is inside bounds and has original color, enqueue it and recolor it",
      "Continue until all reachable original‑color pixels are recolored"
    ],
    "comments": [
      "Early exit avoids infinite loop when new color equals original color",
      "Uses range() checks for bounds each iteration"
    ],
    "time": "O(m*n)",
    "space": "O(m*n)"
  },
  "994": {
    "approach": [
      "Collect positions of all initially rotten oranges and count fresh oranges",
      "Perform BFS level‑by‑level using a deque for the current layer and another for the next layer",
      "For each rotten orange, infect adjacent fresh oranges, enqueue them, decrement fresh count, and mark them rotten",
      "Replace current layer with the next layer and increment elapsed time",
      "After BFS, return -1 if any fresh orange remains, otherwise return elapsed time"
    ],
    "comments": [
      "Uses two separate deques to isolate each BFS level, simplifying time increment logic",
      "Range membership checks (r in range(rows)) create a new range each call; a simple bounds check (0<=r<rows) would be marginally faster"
    ],
    "time": "O(m*n)",
    "space": "O(m*n)"
  },
  "130": {
    "approach": [
      "Check for empty board",
      "Mark all border 'O's as safe and enqueue them",
      "BFS from queue, converting connected 'O's to safe",
      "Flip remaining 'O's to 'X' and revert safe markers to 'O'"
    ],
    "comments": [
      "Uses a temporary marker 'S' to avoid revisiting cells",
      "Queue may grow to O(m*n) in worst case"
    ],
    "time": "O(m*n)",
    "space": "O(m*n)"
  },
  "542": {
    "approach": [
      "Initialize queue with all zero cells and mark others as -1",
      "Perform BFS expanding from zeros",
      "Set each unvisited neighbor to current distance+1 and enqueue it",
      "Repeat until queue is empty"
    ],
    "comments": [
      "In‑place modification uses -1 as unvisited marker",
      "Queue may hold up to O(m·n) cells in worst case"
    ],
    "time": "O(m*n)",
    "space": "O(m*n)"
  },
  "1020": {
    "approach": [
      "Count all land cells (value 1) in the grid",
      "Add every border land cell to a queue, mark it visited, and subtract it from the count",
      "Perform BFS from the queue, visiting 4‑directionally adjacent land cells, marking them visited and subtracting each from the count",
      "Return the remaining count as the number of enclave cells"
    ],
    "comments": [
      "Uses a separate visited set; could reuse the grid to achieve O(1) extra space",
      "Range checks (r in range(rows)) create a new range object each iteration but are still O(1)"
    ],
    "time": "O(rows * cols)",
    "space": "O(rows * cols)"
  }
}